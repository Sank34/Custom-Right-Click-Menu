/*
    SYSTEM FOR CONTEXT-MENU
  - Welcome! Thanks for using my project 🥹
  - This file contains the code in order to make the contextmenu work
  - For assitance, please join our discord server at https://discord.scienceturtles.com
  - For docs, please visit the MDN page for context menu: https://developer.mozilla.org/en-US/docs/Web/API/Element/contextmenu_event
  - Check the readme file for more informations on how to use this project 🐢
*/

// Menu script
  var i = document.getElementById("menu").style; //select the menu div
  if (document.addEventListener) {
    //show menu
    document.addEventListener('contextmenu', function(e) {
      var posX = e.clientX;
      var posY = e.clientY;
      menu(posX, posY);
      e.preventDefault();
    }, false);
    document.addEventListener('click', function(e) {
      i.opacity = "0";
      setTimeout(function() {
        i.visibility = "hidden";
      }, 501);
    }, false);
  } else {
    document.attachEvent('oncontextmenu', function(e) {
      var posX = e.clientX;
      var posY = e.clientY;
      menu(posX, posY);
      e.preventDefault();
    });
    document.attachEvent('onclick', function(e) { 
      i.opacity = "0";
      setTimeout(function() {
        i.visibility = "hidden";
      }, 501);
    });
  }
  
  //get the position for the menu function
  function menu(x, y) {
    i.top = y + "px";
    i.left = x + "px";
    i.visibility = "visible";
    i.opacity = "1";
  }

  /* ============ FUNCTIONS FOR OPTIONS ============== */
  function redirect(link) { //you can also use the "href='...' " , this is just for example
    window.open(link)
  }
  
  